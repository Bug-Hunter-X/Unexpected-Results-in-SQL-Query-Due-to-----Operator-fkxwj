```sql
SELECT * FROM employees WHERE department = 'Sales' AND salary >= 100000;
```
The solution uses the '>=' operator which includes employees with salaries greater than or equal to 100000, thus fixing the issue.